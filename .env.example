# Optional. Origin of the intake API (the portal's public service), inlined at build time.
# Empty means same origin: production is proxied by Cloudflare, the editor preview by the Armory Caddy.
# CI sets https://intake.rothrocklegal.com (.github/workflows/deploy.yml).
NEXT_PUBLIC_INTAKE_API=
